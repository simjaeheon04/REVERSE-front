import styled from "styled-components";
import * as Base from "../ProjectApplyCompletePage/ProjectApplyCompletePage.styles";

export const Page = Base.Page;
export const CompleteSection = Base.CompleteSection;
export const Content = Base.Content;
export const Message = Base.Message;
export const Description = Base.Description;

export const BrandTitle = styled.h1`
  margin: 0;
  color: #f3f5ff;
  font-family: "Jersey 25", "Noto Sans KR", sans-serif;
  font-size: clamp(42px, 6vw, 64px);
  font-weight: 400;
  line-height: 1;
  letter-spacing: 0;
`;
